services:
  - type: web
    name: schoolheadoffice
    env: ruby
    plan: free
    branch: main
    buildCommand: "./render-build.sh"
    startCommand: "bundle exec rails server -p $PORT -e production"
    envVars:
      - key: MONGODB_URI
        sync: false